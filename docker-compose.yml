services:
  db:
    image: postgres:17
    environment:
      POSTGRES_DB: octobird
      POSTGRES_USER: octobird
      POSTGRES_PASSWORD: octobird
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"

volumes:
  pgdata:
